import React from "react";

const ScreenTitle = ({ title }) => {
  return (
    <div className={"text-white font-bold text-4xl mb-6 uppercase"}>
      {title}
    </div>
  );
};

export default ScreenTitle;
